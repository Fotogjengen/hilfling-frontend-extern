import React, { FC } from "react";
import { Typography } from "@material-ui/core";
import cx from "classnames";
import guistyles from "../../styles/utilities.module.css";
import styles from "../MyProfile/MyProfile.module.css";

const NotFound: FC = () => {
  return (
    <div className={}>
      Shoot! Her gikk noe galt... Denne siden ser ikke ut til å eksistere. Hvis
      det er noe du mener er feil kan du sende en mail til{" "}
    </div>
  );
};

export default NotFound;
