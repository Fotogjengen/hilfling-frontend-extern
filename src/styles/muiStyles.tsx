import { createTheme } from "@mui/material";

export const theme = createTheme({
  typography: {
    fontFamily: `"Raleway", sans-serif`,
  },
  palette: {
    primary: {
      main: "#7793DA",
    },
    secondary: {
      main: "#999",
    },
  },
});
